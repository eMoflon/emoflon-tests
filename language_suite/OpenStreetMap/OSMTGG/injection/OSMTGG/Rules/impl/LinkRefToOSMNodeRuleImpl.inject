
partial class LinkRefToOSMNodeRuleImpl {


@model postProcessBACKWARD (Node wayNode, Node refNode, OSMNode oSMNode, OSMWay oSMWay) <--
		refNode.setIndex(oSMWay.getOsmNodes().indexOf(oSMNode));

		if (wayNode.getChildren().size() == oSMWay.getOsmNodes().size()) {
			org.moflon.moca.MocaTreeSorter.sort(wayNode);
		}
-->

}